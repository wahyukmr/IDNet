import React from 'react';
import {View} from 'react-native';
import Navigator from './routes';

const App = () => {
  return (
    <View style={{flex: 1}}>
      <Navigator />
    </View>
  );
};
export default App;
